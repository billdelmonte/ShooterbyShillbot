# On-chain program (future)

v0 pays from a treasury wallet using Solana CLI for speed.

v1 will be a Solana program (Rust) that:
- owns a PDA pot vault
- enforces pot-only spends
- stores window metadata / anchors results
- supports batch payouts

Not required for tonight’s launch.
